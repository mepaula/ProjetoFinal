@model IEnumerable<ProjetoFinal.Models.Entrada>

@{
    ViewData["Title"] = "Index";
}

<h1>Registre Aqui</h1>

<p>
    <a asp-action="Create">Criar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Produto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataEntrada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuantidadeEntrada)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Produto.ProdutoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataEntrada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuantidadeEntrada)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EntradaId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.EntradaId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.EntradaId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
