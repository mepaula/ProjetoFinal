@model IEnumerable<ProjetoFinal.Models.TipoSaida>

@{
    ViewData["Title"] = "Index";
}

<h1>Resgistre Aqui</h1>

<p>
    <a asp-action="Create">Criar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoSaidaDescricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoSaidaDescricao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoSaidaId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.TipoSaidaId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoSaidaId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
